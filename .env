# Site name — used as both the Frappe site name and the nginx header
SITE_NAME=frontend

# Frappe admin password (login at http://localhost:8080 with Administrator / this password)
ADMIN_PASSWORD=admin

# MariaDB root password
DB_ROOT_PASSWORD=admin

# Host port for the web UI (maps to nginx on :8080 inside the container)
HTTP_PORT=8080
